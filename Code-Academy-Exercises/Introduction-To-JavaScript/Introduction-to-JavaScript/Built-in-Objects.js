console.log(Math.floor(Math.random() * 100)); // Picks a random whole number between 0 and 100

console.log(Math.ceil(43.8)); // Returns the smallest integer greater than or equal to a decimal number

console.log(Number.isInteger(2017)); // Checks to see if 2017 is an integer
